Writer-Reviewer Multi-Agent Workflow

This is a minimal local scaffold demonstrating a Writer and Reviewer agent collaborating to produce refined content.

Usage:
```bash
python -m agent_workflow.main "Write a short paragraph about the benefits of unit testing."
```

The app is a simple Python demo and does not require external AI services.
